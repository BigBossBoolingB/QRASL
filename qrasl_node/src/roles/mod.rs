//! The `roles` module defines the different operational modes for a QRASL node.
//! Each submodule corresponds to a specific role a node can take within the network.

pub mod validator;
pub mod solver;
pub mod zk_miner;
